<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.pointematerialx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PointEMaterialX">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".NoteDetailActivity" />
        <activity android:name=".common.SignupActivity" />
        <activity android:name=".common.ForgotPasswordActivity" />
        <activity android:name=".common.LoginActivity" />
        <activity android:name=".SettingPrefActivity" />
        <activity android:name=".AddNoteActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>